version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "fix"
      include: "scope"
    ignore:
      - dependency-name: "next"
        versions: ["> 16.2.3"] # Handled manually to prevent breaking React server changes
